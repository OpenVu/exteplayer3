# الجولة الثانية من الإصلاحات — تقطّع الصوت وتسارع الصورة

هذه الجولة تعالج ثلاثة عيوب بقيت بعد الجولة الأولى (`FIX-HISILICON-QVIART.md`).
كل عيب تم إثباته باختبار قابل للتشغيل، والأرقام أدناه من تشغيل فعلي للكود نفسه.

---

## 1. `WriteWithRetry()` — كان ينتظر قبل أن يكتب

**الملف:** `output/writer/mipsel/writer.c`

**المشكل:** الترتيب كان معكوساً:

```c
select(... , &tv);      /* 50ms */
ret = write(fd, buf, size);
```

الواصف مفتوح بـ `O_NONBLOCK`، فالكتابة الفاشلة لا تكلّف شيئاً وترجع `EAGAIN`.
لكن على أجهزة HiSilicon الجهاز **لا يُبلّغ أبداً** عن جاهزيته للكتابة، فكل حزمة
كانت تدفع مهلة الـ 50ms كاملة قبل أن تُكتب.

**الإصلاح:** الكتابة أولاً، و `select()` فقط عند `EAGAIN`/`EWOULDBLOCK` الحقيقي.

**التحقّق** — جهاز وهمي يقبل كل الكتابات لكنه لا يُبلّغ أبداً عن الجاهزية،
70 حزمة (≈ ثانية واحدة من الوسائط):

| | قبل | بعد |
|---|---|---|
| الزمن | 3512 ms | 0 ms |
| استدعاءات `select()` | 70 | 0 |
| **زمن حقيقي لكل ثانية وسائط** | **3.51 s** | **0.00 s** |

**اختبار عدم الانحدار** — مستهلك بطيء حقيقي (أنبوب يُفرَّغ ببطء، 512 KB):

| | قبل | بعد |
|---|---|---|
| البايتات المسلَّمة | 524288/524288 | 524288/524288 |
| سلامة المحتوى | مطابق | مطابق |
| استدعاءات `select()` | 119 | 53 |

---

## 2. `OutputPacingWait()` — الساعة كانت تسرّب 1.5 ثانية كل دقيقة

**الملفات:** `output/writer/common/misc.c`، `include/misc.h`

**المشكل:** كشف الانقطاع كان يقارن الـ PTS مع **المرساة الثابتة**:

```c
dpts = pts - g_pacing_pts;
if (dpts < -90000LL || dpts > 90000LL * 60LL) { ... }   /* دائماً صحيح بعد 60s */
```

في أي ستريم متواصل يتحقّق الشرط مرة كل 60 ثانية، وكل إعادة ضبط كانت تُسنِد
`wall = now` فتمنح 1.5 ثانية تقدّم جديدة. التقدّم يتراكم بلا سقف.

**الإصلاح:**

- `last_pts` لكل ستريم، والانقطاع يُقاس مقابل **الحزمة السابقة** لا المرساة.
- عند إعادة الضبط الحقيقية: `wall = now + maxAhead` بدل `now` — حفاظاً على
  الإزاحة الحالية بدل منح تقدّم جديد.
- ساعة مستقلة لكل ستريم (`PACING_STREAM_VIDEO` / `PACING_STREAM_AUDIO`)،
  لأن الصوت والصورة صارا يُحقنان من خيطين مختلفين، ولأن انقطاعاً في أحدهما
  يجب ألّا يُفسد توقيت الآخر.

**التوقيع الجديد:**

```c
void OutputPacingWait(void *context, int32_t stream, int64_t pts);
```

**التحقّق** — محاكاة بساعة وهمية:

| السيناريو | قبل | بعد |
|---|---|---|
| ساعة واحدة متواصلة (25 fps) | +92.4 s (**+2.63%**) | +1.5 s (+0.04%) |
| عدد إعادات الضبط | 59 | 0 |
| صوت وصورة متداخلان (فارق 800ms) | — | +1.5 s (+0.04%) |
| 5 انقطاعات حقيقية (+5s لكل واحدة) | — | +1.5 s (+0.04%) |
| التفاف PTS على 33 بت | — | +1.6 s، إعادة ضبط واحدة |

التقدّم يبقى ثابتاً عند 1.5 ثانية مهما طال التشغيل.

---

## 3. فصل الصوت عن الصورة — طابور وخيط لكل ستريم

**الملف:** `output/linuxdvb_buffering.c` (إعادة هيكلة)

**المشكل — وهو الفرق البنيوي الأساسي مع gstplayer:**

كان هناك **طابور FIFO واحد وخيط كتابة واحد** للصوت والصورة معاً. أي تأخّر في
كتابة حزمة فيديو (بافر الديكودر ممتلئ، `select()` بطيء، انتظار التوقيت) يوقف
كل حزم الصوت الواقفة خلفها.

في gstplayer المسارات منفصلة: `dvbvideosink` و `dvbaudiosink` لكل منهما خيط
تدفّق خاص، فتعطّل الفيديو لا يمسّ الصوت إطلاقاً.

**الإصلاح:** بنية `StreamQueue_t` — طابور + خيط + أنبوب إيقاظ + متغيّرات تزامن
لكل ستريم على حدة. تقسيم بافر الـ RAM: 7/8 للفيديو و 1/8 للصوت (الصوت يغطّي
عشرات الثواني رغم حصّته الصغيرة بسبب معدّل بتّه المنخفض).

الواجهة العامة لم تتغيّر إطلاقاً (`LinuxDvbBuffOpen/Close/Flush/Resume/
SetSize/GetSize/SetStamp/SetPts/BufferingWriteV`).

**أخطاء إضافية أُصلحت أثناء إعادة الهيكلة:**

- `static BufferingNode_t *nodePtr` داخل خيط الكتابة — متغيّر ساكن يُشارَك بين
  خيطين الآن، كان سيؤدي إلى تحرير مزدوج. صار محلّياً.
- `pthread_mutex_init` / `pthread_cond_init` كانت تُستدعى **بعد**
  `pthread_create` (سباق). صارت قبله.
- `pfd` كان يُهيَّأ إلى `{0,0}` ضمنياً، فـ `WriteWakeUp()` قبل انطلاق الخيط
  كانت تكتب إلى الواصف 0 (stdin). صار `{-1,-1}` صراحةً.
- `LinuxDvbBuffClose` كانت ستنتظر 2 ثانية لكل خيط بالتسلسل (4 ثوانٍ). صارت
  تطلب الإيقاف من الاثنين أولاً ثم تنتظر بمهلة مشتركة.

**التحقّق** — جهاز فيديو وهمي بطيء (50ms لكل حزمة)، 100 حزمة فيديو + 100 حزمة
صوت، قياس ما وصل بعد ثانية واحدة:

| | قبل (طابور واحد) | بعد (طابوران) |
|---|---|---|
| الصوت المسلَّم | 9500 / 50000 بايت (**19%**) | 50000 / 50000 بايت (**100%**) |

**اختبار ضغط** (4000 حزمة لكل ستريم + `LinuxDvbBuffFlush` متزامن 40 مرة، تحت
AddressSanitizer): لا تجمّد، لا تعطّل، لا أخطاء ذاكرة — والنتيجة مطابقة للنسخة
القديمة في `LinuxDvbBuffClose`.

---

## 4. الملفات المعدَّلة في هذه الجولة

| الملف | التعديل |
|---|---|
| `output/writer/mipsel/writer.c` | `write()` أولاً، `select()` فقط عند `EAGAIN` |
| `output/writer/common/misc.c` | ساعة توقيت لكل ستريم + كشف انقطاع صحيح + إعادة ضبط تحفظ الإزاحة |
| `include/misc.h` | `PACING_STREAM_*` + التوقيع الجديد لـ `OutputPacingWait` |
| `output/linuxdvb_buffering.c` | طابور وخيط كتابة مستقلان للصوت والصورة |
| `container/container_ffmpeg.c` | تمرير نوع الستريم إلى `Write()` / `OutputPacingWait()` |
| `container/mpeg4p2_ffmpeg.c` | نفس التمرير |
| `container/flv2mpeg4_ffmpeg.c` | نفس التمرير |

**السلوك على أجهزة Broadcom / VU / Dreambox لم يتغيّر:**
`OutputPacingLeadMs()` يرجع 0 عليها فالتوقيت معطّل، وبافر الـ RAM لا يُفعَّل
تلقائياً، و `select()` يعمل بشكل صحيح فالمسار الجديد يكتب مباشرة كما كان.

---

## 5. الاختبار على الجهاز

```sh
init 4

# تشغيل عادي (توقيت تلقائي + بافر 16MB)
exteplayer3 "http://server/stream.m3u8"

# لاحظ في اللوغ:
#   Detected STB type: 3   ← لازم يكون 3 (hisilicon)
#   bufferSize [16777216] pacing lead [1500 ms]

# إذا ظهر 999 فالكشف عن الشريحة فشل، أجبر التوقيت يدوياً:
exteplayer3 -Y 1500 "http://server/stream.m3u8"

# للمقارنة مع السلوك القديم (يعود التسارع):
exteplayer3 -Y 0 "http://server/stream.m3u8"

init 3
```

إذا بقي تقطّع بسيط عند ضعف الشبكة، زِد البافر: `-b 32`.
إذا أردت تبديل قنوات أسرع على حساب مقاومة التذبذب: `-Y 800`.
