# تشخيص وحل مشكلة تسارع الصورة والصوت في exteplayer3 على QVIART DUAL 4K

## 1. التشخيص

جهاز QVIART DUAL 4K مبني على شريحة **HiSilicon** (Hi3798 / Cortex-A53).

### أين المشكل بالضبط

`exteplayer3` **لا يملك أي آلية توقيت برمجية** (no software pacing). طريقة عمله:

```
ffmpeg يقرأ الحزمة  ->  writer يبني PES  ->  write() إلى /dev/dvb/adapter0/video0
```

سرعة التشغيل تُحدَّد بالكامل من طرف الدرايفر: عندما يمتلئ بافر الديكودر، يُفترض أن
ترجع `write()` بـ `EAGAIN` ويتوقف `select()` في `output/writer/mipsel/writer.c`
حتى يستهلك العتاد البيانات في الزمن الحقيقي. هذا يشتغل جيداً على Broadcom و STi.

على أجهزة HiSilicon، أجهزة `/dev/dvb/adapter0/video0` و `audio0` ليست حقيقية بل
**طبقة محاكاة (emulation)** فوق HiSilicon media API:

- تبتلع كل ما يُكتب إليها دون أن ترجع "البافر ممتلئ" أبداً → لا يوجد back-pressure.
- الـ PTS المحقون في رأس PES لا يقود ساعة العرض.
- بالإضافة إلى ذلك كان `output/linuxdvb_mipsel.c` يفرض
  `ioctl(audiofd, AUDIO_SET_AV_SYNC, 0)` أي **تعطيل مزامنة A/V** بشكل ثابت.

النتيجة: يُحقن الستريم بأقصى سرعة يسمح بها الإنترنت → الصورة والصوت يتسارعان.

### لماذا gstplayer (5001) يشتغل بشكل صحيح

`gstplayer` يستعمل `dvbvideosink` / `dvbaudiosink` وهما `GstBaseSink` بخاصية
`sync=true`: كل بافر ينتظر ساعة GStreamer قبل دفعه إلى الجهاز. أي أن التوقيت
يتم **برمجياً** داخل GStreamer ولا يعتمد على الدرايفر. لهذا لا تظهر المشكلة.

### ملاحظة إضافية

دالة `GetSTBType()` كانت تتعرّف على HiSilicon عبر مسار وحيد
(`/sys/firmware/devicetree/base/soc/hisilicon_clock/name`) وهو غير موجود على كل
الأجهزة. لذلك حتى المعالجات الخاصة بـ HiSilicon الموجودة أصلاً في الكود
(AAC، MJPEG، VP9، bcma) كانت معطّلة على هذا الجهاز.

---

## 2. الحل المطبَّق

### أ. ساعة تشغيل برمجية (الإصلاح الأساسي)

**الملف:** `output/writer/common/misc.c` (+ التصريحات في `include/misc.h`)

أُضيفت وحدة توقيت تحقن البيانات بحيث لا نتقدّم أبداً أكثر من `lead` ميلي ثانية
عن الزمن الحقيقي، تماماً كما يفعل `dvbvideosink`:

- `monotonic_us()` — ساعة `CLOCK_MONOTONIC`.
- `OutputPacingWait()` — تنتظر حتى يحين وقت حقن الحزمة.
- مرساة (anchor) تربط أول PTS بأول قراءة للساعة، ثم:
  `target = wall0 + (pts - pts0) / 90` بالميلي ثانية.
- إعادة ضبط المرساة تلقائياً عند: seek، انقطاع PTS، أو التفاف 33 بت.
- تجميد الساعة أثناء PAUSE حتى لا يحدث اندفاع بعد الاستئناف.
- النوم على شرائح 50ms حتى يبقى stop / seek متجاوباً.

القيمة الافتراضية: **auto** → 1500ms على أجهزة HiSilicon، **معطّلة** على غيرها
(أي أن سلوك Broadcom/VU/Dreambox لم يتغيّر إطلاقاً).

### أ-2. أين تتم عملية الانتظار (مهم جداً)

في النسخة الأولى كان الانتظار داخل خيط ffmpeg نفسه، وهذا سبب المشكل المتبقي:
عندما ينام خيط القراءة، **لا تبقى أي قراءة مسبقة (read ahead)**، فأي تعثّر بسيط
في الشبكة يُجوّع الديكودر مباشرة → تشويش قصير في الصورة.

الحل: نقل الانتظار إلى **جهة الاستهلاك**:

- `output/linuxdvb_buffering.c` — خيط الكتابة (`LinuxDvbBuffThread`) هو من
  ينتظر الآن قبل كل `WriteWithRetry`. أُضيف حقل `pts` إلى `BufferingNode_t`
  ودالة `LinuxDvbBuffSetPts()`.
- `container/container_ffmpeg.c` — دالة `Write()` تمرّر الـ PTS إلى طبقة
  البافر عند تفعيله، وتنتظر محلياً فقط إذا كان البافر معطّلاً.
- `main/exteplayer.c` — تفعيل بافر الـ RAM بحجم **16 MB** تلقائياً عندما يكون
  التوقيت مفعّلاً ولم يمرّر المستخدم `-b`. (ServiceApp لا يمرّر `-b` أبداً،
  وهذا سبب بقاء المشكل.)

النتيجة: ffmpeg يقرأ من الشبكة بأقصى سرعة ويملأ بافر الـ RAM، بينما الحقن إلى
الديكودر يبقى في الزمن الحقيقي. مجموع الحماية من تذبذب الشبكة = مدة بافر
الـ RAM + 1.5 ثانية.

### أ-5. متغيّر `stamp` غير مهيّأ — سبب تعطّل البافر تماماً

**الملف:** `container/container_ffmpeg.c` — دالة `FFMPEGThread()`

```c
void *stamp;                      /* لم يُهيَّأ أبداً */
...
if (bufferSize > 0)
    LinuxDvbBuffSetStamp(stamp);  /* يُستعمل في كل دورة */
...
if (g_do_seek_target_seconds)
    stamp = g_stamp;              /* يُسنَد فقط عند الـ seek */
```

و في `output/linuxdvb_buffering.c` يفحص خيط الكتابة:

```c
if (nodePtr && !isSeeking && context->playback->stamp == nodePtr->stamp)
```

قيمة `context->playback->stamp` الابتدائية هي `NULL` (في `playback.c`)، ولا
تتغيّر إلا عند أول seek. لكن `stamp` المحلي كان يحمل **قيمة عشوائية من الـ
stack** قبل أول seek، فلا يتطابقان أبداً → **كل قطعة بيانات تُرمى ولا يُكتب أي
بايت إلى الديكودر**.

هذا يفسّر بدقة نتيجة `-b 32`: لا صوت ولا صورة رغم أن كل شيء آخر يبدو سليماً.
وهو خطأ موجود في المصدر الأصلي (سلوك غير معرَّف)، لكنه لا يظهر إلا إذا صادف
أن القيمة العشوائية لم تكن صفراً.

**الإصلاح:** `void *stamp = NULL;` + إسناده من `context->playback->stamp` عند
بدء الخيط.

**التحقق:** تشغيل `linuxdvb_buffering.c` الفعلي مع خيط كتابة وهمي:

| قيمة stamp | البايتات التي وصلت |
|------------|--------------------|
| عشوائية (قبل) | **0** / 51200 |
| مطابقة (بعد) | 51200 / 51200 ✅ |

### أ-4. `select()` بلا مهلة يوقف التشغيل كلياً عند تفعيل البافر

**الملف:** `output/writer/mipsel/writer.c` — دالة `WriteWithRetry()`

للمشغّل مساران مختلفان تماماً للكتابة:

| المسار | الدالة | الآلية |
|--------|--------|--------|
| بدون بافر | `writev_with_retry` → `write_with_retry` | `write()` مباشرة + `usleep` عند `EAGAIN` |
| مع بافر RAM | `WriteWithRetry` | `select()` انتظاراً لجاهزية الجهاز للكتابة |

`select()` كان يُستدعى **بلا مهلة**:

```c
retval = select(maxFd + 1, &rfds, &wfds, NULL, NULL); //&tv);
```

على أجهزة HiSilicon، `/dev/dvb/adapter0/video0` مجرد محاكاة ولا تُبلّغ أبداً
عن جاهزيتها للكتابة، فيبقى `select()` معلّقاً إلى الأبد ولا يُحقن أي بايت →
**الستريم لا يبدأ إطلاقاً**. لهذا انكسر التشغيل بمجرد تفعيل البافر تلقائياً.

المفارقة أن كاتب الكود وثّق هذا الحل بنفسه في تعليق ثم تركه معطّلاً:

```
*   2. make timeout select(), limit max time spend in the select()
*      to for example 0,1s
*   (at now first workaround is used)
```

**الإصلاح:** مهلة 50ms، وعند انتهائها تُجرَّب الكتابة مباشرة — الواصف مفتوح
بـ `O_NONBLOCK` فأسوأ حالة هي `EAGAIN` ودورة جديدة. هذا لا يغيّر شيئاً على
أجهزة Broadcom حيث `select()` يعمل بشكل صحيح.

**التحقق:** باستدعاء `select()` مزيّف لا يُبلّغ أبداً عن الجاهزية:

| | قبل | بعد |
|---|---|---|
| 256 KB عبر جهاز لا يستجيب لـ select | تعليق أبدي | 262144/262144 ✅ |
| 512 KB عبر مستهلك بطيء (حالة عادية) | يعمل | 524288/524288 بلا تلف ✅ |

### أ-3. تجاوز مصفوفة iovec في كاتبي H.264 / H.265

**الملفات:** `output/writer/mipsel/h264.c`، `output/writer/mipsel/h265.c`

هذا خطأ حقيقي منفصل وسبب مباشر لتشويش الصورة أحياناً:

```c
struct iovec iov[128];
...
if (ic >= IOVEC_SIZE) { ... break; }   /* الفحص قبل إضافة عنصرين! */
iov[ic++] = start_code;
iov[ic++] = nal_payload;
```

كل وحدة NAL تستهلك عنصرين. الفحص كان يتم **قبل** إضافتهما، لذلك عند
`ic == 127` يمرّ الفحص ثم تُكتب `iov[128]` → **كتابة خارج حدود المصفوفة على
الـ stack**، ثم يُرمى باقي الإطار:

- في `h264.c`: `break` صامت → نصف الإطار مفقود → تشويش.
- في `h265.c`: `exit(-1)` → إنهاء المشغّل بالكامل.

يحدث هذا مع أي access unit فيه 64 وحدة NAL أو أكثر (شائع في MKV المشفّر
بعدّة شرائح slices، وفي 4K).

**الإصلاح:** بدل الإسقاط، يتم إفراغ الدفعة (`WriteV`) والمتابعة. رأس PES
يُكتب مرة واحدة بطول غير محدّد (unbounded)، لذلك الدفعات التالية هي مجرد
استكمال لنفس حزمة PES ولا تحتاج رأساً جديداً.

**التحقق:** إطار اصطناعي بـ 200 وحدة NAL:

| | قبل | بعد |
|---|---|---|
| وحدات NAL مكتوبة | ~63 (الباقي مُسقط) | 200/200 |
| كتابة خارج الحدود | نعم (`iov[128]`) | لا |
| رؤوس PES | 1 | 1 |
| استدعاءات WriteV | 1 | 4 |

### ب. تفعيل مزامنة A/V على HiSilicon

**الملف:** `output/linuxdvb_mipsel.c` — دالة `LinuxDvbAVSync()`

```c
int32_t avSync = (STB_HISILICON == GetSTBType()) ? context->playback->AVSync : 0;
ioctl(audiofd, AUDIO_SET_AV_SYNC, avSync);
```

الصفر بقي كما هو على باقي الأجهزة (السلوك التاريخي) لتفادي أي انحدار.

### ج. تحسين التعرّف على شرائح HiSilicon

**الملف:** `output/writer/common/misc.c`

أصبح `GetSTBType()` يفحص:

- `/sys/firmware/devicetree/base/soc/hisilicon_clock/name`
- `/proc/hisi`، `/dev/hi_mem`، `/dev/hi_dmx`، `/sys/module/hi_media`
- محتوى `/proc/stb/info/chipset` (يبدأ بـ `hi3` أو يحتوي `hisi`)

### د. خيار جديد في سطر الأوامر

**الملف:** `main/exteplayer.c`

```
-Y <ms>    -1 = تلقائي (الافتراضي)
            0 = تعطيل التوقيت البرمجي
           >0 = أقصى تقدّم مسموح به بالميلي ثانية
```

كما يطبع البرنامج عند الإقلاع نوع الجهاز المكتشف:
`Detected STB type: 3` يعني HiSilicon.

---

## 3. البناء والاختبار

البناء لا يحتاج أي تعديل في `exteplayer3_git.bb` (لم تُضف ملفات جديدة).

بعد النسخ إلى الجهاز، اختبر يدوياً عبر telnet/ssh:

```sh
# أوقف enigma2 أولاً
init 4

# تشغيل عادي (توقيت تلقائي)
exteplayer3 "http://server/stream.m3u8"

# للتأكد أن الكشف عن الشريحة يعمل، لاحظ السطر:
#   Detected STB type: 3 (1=dreambox 2=vuplus 3=hisilicon 999=other)

# إذا ظهر 999 فالكشف فشل — أجبر التوقيت يدوياً:
exteplayer3 -Y 1500 "http://server/stream.m3u8"

# لمقارنة السلوك القديم:
exteplayer3 -Y 0 "http://server/stream.m3u8"

init 3
```

### ضبط قيمة `-Y`

| القيمة | الأثر |
|--------|-------|
| 800–1000 | تبديل قنوات أسرع، لكن حساسية أكبر للتقطيع عند ضعف الشبكة |
| **1500** | الافتراضي — توازن جيد |
| 2500–3000 | مقاومة أعلى لتذبذب الشبكة، بداية تشغيل أبطأ قليلاً |

### إذا بقيت مشكلة — سلالم التراجع

كل مستوى يعزل جزءاً من التغييرات:

```sh
# 1) التشغيل الافتراضي: توقيت + بافر 16MB
exteplayer3 "URL"

# 2) توقيت بدون بافر RAM (يعمل، لكن قد يظهر تشويش عند تعثّر الشبكة)
exteplayer3 -b 0 "URL"

# 3) تعطيل التوقيت كلياً (يعود التسارع - للتأكد أن التوقيت هو الفاعل)
exteplayer3 -Y 0 "URL"

# 4) بافر أكبر عند ضعف الاتصال
exteplayer3 -b 32 "URL"
```

`-b 0` يعني الآن فعلاً "بدون بافر" ولا يُستبدل بالقيمة التلقائية.

### حجم بافر الـ RAM

يُفعَّل تلقائياً بـ 16 MB عند تفعيل التوقيت. لزيادته عند ضعف الاتصال:

```sh
exteplayer3 -b 32 "http://server/movie.mkv"
```

### عبر ServiceApp

ServiceApp يُشغّل الأمر هكذا (كما في اللوغ):

```
exteplayer3 "http://..." -u "Mozilla/5.0" -a 0 -n 0
```

لا يمرّر `-Y` ولا `-b`، لذلك تم جعل الاثنين يُفعَّلان تلقائياً على HiSilicon.
لا تحتاج أي إعداد إضافي.

---

## 4. الملفات المعدَّلة

| الملف | التعديل |
|-------|---------|
| `output/writer/mipsel/writer.c` | مهلة لـ `select()` في `WriteWithRetry` (بدونها لا يشتغل الستريم مع البافر) |
| `output/writer/common/misc.c` | وحدة التوقيت البرمجي + كشف موسّع لشرائح HiSilicon |
| `include/misc.h` | تصريحات `OutputPacing*` |
| `output/linuxdvb_buffering.c` | التوقيت داخل خيط الكتابة + حقل `pts` في العقدة |
| `container/container_ffmpeg.c` | تهيئة `stamp` (كانت تُعطّل البافر كلياً) + تمرير الـ PTS لطبقة البافر / الانتظار محلياً عند تعطيله + إعادة الضبط عند seek |
| `output/writer/mipsel/h264.c` | إصلاح تجاوز `iov[]` وفقدان NAL units |
| `output/writer/mipsel/h265.c` | نفس الإصلاح + إزالة `exit(-1)` |
| `output/linuxdvb_mipsel.c` | `AUDIO_SET_AV_SYNC` مفعّل على HiSilicon |
| `main/exteplayer.c` | خيار `-Y` + بافر 16MB تلقائي + طباعة نوع الجهاز |
